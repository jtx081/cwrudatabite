class IVisual {
    useDataSet(data) {
        this.data = data
    }
    setXAxisTitle(xTitle) {
        this.xAxisTitle = xTitle
    }
    setYAxisTitle(yTitle) {
        this.yAxisTitle = yTitle
    }
    setLegend(description) {
        this.legend = description
    }
}
